<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Blog - All Posts</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="container">
    <div class="terminal-header">
      <span class="command-prompt">$</span><span class="prompt-path">~/portfolio/blog</span> ls -la
    </div>
    
    <div class="admin-header">
      <h1>Blog</h1>
      {% if isAdmin %}
        <form action="/admin/logout" method="GET" style="margin: 0;">
          <button type="submit" class="logout-btn">logout</button>
        </form>
      {% else %}
        <a href="/admin/login" style="color: #58a6ff; text-decoration: none; font-weight: normal; font-size: 14px;">admin login</a>
      {% endif %}
    </div>
    
    <nav>
      <a href="/">home</a>
      <a href="/blog">blog</a>
      <a href="https://github.com/RikoxCode" target="_blank">github</a>
    </nav>
    
    {% if isAdmin %}
    <section class="admin-only">
      <h2>new post</h2>
      <form action="/blog" method="POST">
        <input type="text" name="title" placeholder="Title" required>
        <textarea name="content" placeholder="Write your post content..." required rows="8"></textarea>
        <button type="submit">publish</button>
      </form>
    </section>
    {% endif %}

    <section class="portfolio-section">
      <h2>all posts</h2>
      {% if posts.length > 0 %}
        <ul class="post-list">
          {% for post in posts %}
            <li>
              <div style="display: flex; justify-content: space-between; align-items: center;">
                <div>
                  <a href="/blog/{{ post._id }}">{{ post.title }}</a>
                  <br><small>{{ post.createdAt.toLocaleDateString('en-US') }} at {{ post.createdAt.toLocaleTimeString('en-US', { hour: '2-digit', minute: '2-digit' }) }}</small>
                </div>
                {% if isAdmin %}
                  <form action="/blog/{{ post._id }}/delete" method="POST" style="margin: 0;" onsubmit="return confirm('Delete this post and all comments?');">
                    <button type="submit" style="background: transparent; color: #f85149; border-color: #da3633; padding: 4px 10px; font-size: 12px;">delete</button>
                  </form>
                {% endif %}
              </div>
            </li>
          {% endfor %}
        </ul>
      {% else %}
        <p style="color: #7d8590;">No posts yet. Create the first one!</p>
      {% endif %}
    </section>
  </div>
</body>
</html>
